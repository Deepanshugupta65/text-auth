GET /tasks
     │
     ▼
   FastAPI
     │
     ▼
 Check Redis
  │       │
  │ HIT   │ MISS
  ▼       ▼
Redis   PostgreSQL
  │       │
  │       ▼
  │      Save result to Redis
  │       │
  └───────┘
      ↓
   Response


   // cache invaildation 

   create_task()
    ↓
clear_task_cache()

update_task()
    ↓
clear_task_cache()

delete_task()
    ↓
clear_task_cache()

we are creating cache invalidation specifically to prevent Redis from returning old task data after PostgreSQL is changed.