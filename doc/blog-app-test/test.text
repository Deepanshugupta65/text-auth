TEST-AUTH.python

Register
Login
Wrong password
Duplicate email
JWT authentication
Unauthorized request

-------------------------------
test_task.py

Create task
Get tasks
Get only current user's tasks
Delete task
Delete someone else's task
Update task
Invalid task ID

---------------------------------
conftest.py


Test database
FastAPI TestClient
Test user
Authentication token
Database session


----------------------------------------------------
FastAPI app = Restaurant
Client       = Customer
API          = Waiter
Database     = Kitchen/Store room
Test         = Inspector  

why using test 
let me check wheather my application is doing what i told it to do

step1:
pip install pytest httpx
pytest =  teacher who checks your answers
https = Customer who can talk to your API

theory part: 
then i created a tests folder after this test_main.py
archiectrue of this :
-------------------
pytest
  ↓
runs test_home()
  ↓
calls your FastAPI /
  ↓
gets the correct response
  ↓
✅ PASS

quesyion : why do we need testing
ans - imagine your appplication has 50 apis . you change one thing in your login cide now we world have to manullay check register , login, task and all the feature with test it can test all the test run automatically 

concept :
real database  - textauth application - imp data
test database - textauth_test - playground

Tests can freeely create and delete things in textauth_test without touching your real database

---------------------------------------------
step 2:
my real application use textauth 
for textauth_test we create textauth_test databae in pgadmin
 we will tell pytest when i am testing use textauth_test 

 -------------------------

 step 3:
 we created conftest.py
 like in  session.py  -> textauth
           conftest -> textauth_test
Normal app :
    FastAPI  -> textauth
During test :
     FastAPI  -> textauth_test

--------------------------------------------        

Step 4 :

make the table for test database 
for the user and tasks in textauth_test database

then fastapi the to test database 
write this on confest.test.py 

def override_get_db():
    db = TestingSessionLocal()
    try:
        yield db
    finally:
        db.close()

 ===== 
 Normally:

API → get_db() → textauth


During pytest:

API → get_db() → 🚧 "Wait!"
                 ↓
          override_get_db()
                 ↓
            textauth_test

step 5:

tests/ test_database.py
--------------------------------------------
step 6:

test_auth.py  
this is auth flow 
/register
    ↓
create user
    ↓
hash password
    ↓
save in DB

/login
    ↓
find user
    ↓
check password
    ↓
create JWT
    ↓
return token

/logout
    ↓
return message


-------------step 6 :
register and duplicate email
test_register_user()
        ↓
create user
        ↓
✅ 200


test_duplicate_email()
        ↓
create user first
        ↓
try same email again
        ↓
✅ 400




for learing testing them in this order :
AUTH
│
├── 1. Register ✅
├── 2. Duplicate email
├── 3. Login success
├── 4. Login wrong password
├── 5. JWT protected route
├── 6. Missing/invalid JWT
├── 7. Logout
│
TASK
│
├── 8. Create task
├── 9. Get tasks
├── 10. Pagination
├── 11. Search
├── 12. Filtering
├── 13. Update task
├── 14. Delete task
│
AUTHORIZATION
│
├── 15. User can access own task
├── 16. User cannot access another user's task
└── 17. Admin can access/manage all tasks